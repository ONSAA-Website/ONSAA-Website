# build output
dist/
.astro/

# dependencies
# package-lock.json is intentionally NOT ignored: CI runs `npm ci`, which
# requires a committed lockfile.
node_modules/
.yarnrc.yml
.yarn/

# macOS system files
.DS_Store
# node.js & npm
node_modules/
package-lock.json
.yarnrc.yml
.yarn/
# stripe
server/.env
server/.env.*