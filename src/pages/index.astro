---
import Layout from "../layouts/Layout.astro";
import { url } from "../lib/url";
import trilliumImage from '../assets/trillium.png';
import SocialLinks from "../components/SocialLinks.astro";

const targetDateTime = "2026-11-05T08:30:00-05:00";
const appsScriptUrl =
  "https://script.google.com/macros/s/AKfycbwYCrYRibeVLlQCdUmOp09jWq_cqdqOaocRx8S18x6qwD9Su57bKO6bvhK0DWwOYWPR/exec";
---

<Layout
  title="Ontario Students Against Austerity"
  description="The ONSAA Landing Page."
  current="/"
  variant="hero"
>
  <div class="home-page">
  <section class="content" aria-label="Landing page introduction">
    <div class="hero-layout">
      <div class="hero-intro">
        

       <pre class="ascii-title" aria-label="Ontario Students Against Austerity">
           .d88888b.  888b    888 88888888888     d8888 8888888b.  8888888 .d88888b.       .d8888b. 88888888888 888     888 8888888b.  8888888888 888b    888 88888888888 .d8888b.               
          d<span class="ascii-flicker flicker-a flicker-node">88P</span>" "Y88b 8888b   888     <span class="ascii-flicker flicker-b flicker-node">888</span>        d88888 888   Y88b   888  d88P" "Y88b     d88P  Y88b    888     888     888 888  "Y88b 888        8888b   888     <span class="ascii-flicker flicker-h flicker-node">888</span>    d88P  Y88b              
          888     888 88888b  888     <span class="ascii-flicker flicker-b flicker-node">888</span>       d88P888 888    888   <span class="ascii-flicker flicker-c flicker-node">888</span>  888     888     Y88b.         888     888     888 888    888 888        88888b  888     888    Y88b.                   
          888     888 888Y88b 888     888      d88P 888 888   d88P   <span class="ascii-flicker flicker-c flicker-node">888</span>  888     888      "Y888b.      <span class="ascii-flicker flicker-d flicker-node">888</span>     888     888 888    888 8888888    888Y88b 888     888     "Y888b.                
          888     888 888 Y88b888     888     d88P  888 8888888P"    888  888     888         "Y88b.    <span class="ascii-flicker flicker-d flicker-node">888</span>     888     888 888    888 888        <span class="ascii-flicker flicker-e flicker-node">888</span> Y88b888     888        "Y88b.              
          888     888 <span class="ascii-flicker flicker-f flicker-node">888</span>  Y88888     888    d88P   888 888 T88b     888  888     888           "888    888     888     888 888    888 888        <span class="ascii-flicker flicker-e flicker-node">888</span>  Y88888     888          "888              
          Y88b. .d88P <span class="ascii-flicker flicker-f flicker-node">888</span>   Y8888     888   d8888888<span class="ascii-flicker flicker-g flicker-node">888</span> 888  T88b    888  Y88b. .d88P     Y88b  d88P    888     Y88b. .d88P 888  .d88P 888        888   Y8888     888    Y88b  d88P              
           "Y88888P"  888    Y888     888  d88P     888 888   T88b 8888888 "Y88888P"       "Y8888P"     888      "Y88888P"  8888888P"  8888888888 888    Y888     888     "Y8888P"               
                                                                                                                                                                                       
                                                                                                                                                                                       
                                                                                                                                                                                       
       d8888  .d8888b.         d8888 8888888 888b    888  .d8888b. 88888888888            d8888 888     888  .d8888b. 88888888888 8888888888 8888888b.  8888888 88888888888 Y88b   d88P
      d<span class="ascii-flicker flicker-i flicker-node">888</span>88 d88P  Y88b       d88888   888   8888b   888 d88P  Y88b    888               d88888 888     888 d88P  Y88b    888     888        888   Y88b   <span class="ascii-flicker flicker-o flicker-node">888</span>       888      Y88b d88P 
     d8<span class="ascii-flicker flicker-i flicker-node">8P8</span>88 888    888      <span class="ascii-flicker flicker-j flicker-node">d88</span>P888   888   88888b  888 Y88b.         888              d88P888 888     888 Y88b.         888     888        888    888   <span class="ascii-flicker flicker-o flicker-node">888</span>       888       Y88o88P  
    d88P 888 888            d<span class="ascii-flicker flicker-j flicker-node">88P</span> 888   888   <span class="ascii-flicker flicker-k flicker-node">888</span>Y88b 888  "Y888b.      888             d88P 888 888     888  "Y888b.      888     8888888    888   d88P   888       888        Y<span class="ascii-flicker flicker-p flicker-node">888</span>P   
   d88P  888 888  88888    d88P  888   888   <span class="ascii-flicker flicker-k flicker-node">888</span> Y88b888     "Y88b.    <span class="ascii-flicker flicker-l flicker-node">888</span>            d88P  888 888     888     "Y88b.    888     888        8888888P"    888       888         <span class="ascii-flicker flicker-p flicker-node">888</span>    
  d88P   888 888    888   d88P   888   888   888  Y88888       "888    <span class="ascii-flicker flicker-l flicker-node">888</span>           d88P   888 <span class="ascii-flicker flicker-m flicker-node">888</span>     888       "888    888     888        888 T88b     888       888         888    
 d8888888888 Y88b  d88P  d8888888888   888   888   Y8888 Y88b  d88P    888          d8888888888 <span class="ascii-flicker flicker-m flicker-node">Y88</span>b. .d88P Y88b  d88P    <span class="ascii-flicker flicker-n flicker-node">888</span>     888        888  T88b    888       888         888    
d88P     888  "Y8888P88 d88P     888 8888888 888    Y888  "Y8888P"     888         d88P     888  "Y88888P"   "Y8888P"     888     8888888888 888   T88b 8888888     888         888    
</pre>

      </div>

      <div class="strike-countdown">

        <div class="countdown" aria-label="Countdown to the student strike">
          <div class="countdown-card">
            <span class="countdown-number" id="days">00</span>
            <span class="countdown-label">Days</span>
          </div>

          <div class="countdown-card">
            <span class="countdown-number" id="hours">00</span>
            <span class="countdown-label">Hours</span>
          </div>

          <div class="countdown-card">
            <span class="countdown-number" id="minutes">00</span>
            <span class="countdown-label">Minutes</span>
          </div>

          <div class="countdown-card">
            <span class="countdown-number" id="seconds">00</span>
            <span class="countdown-label">Seconds</span>
          </div>
        </div>
        <p class="countdown-heading">until the student strike.</p>
      </div>

      <div class="petition-row">
        <a class="button" href={url("/petition/")}>
          Sign the petition
        </a>

        <p class="signature-panel" aria-label="Petition signatures">
          <span class="status-dot" aria-hidden="true"></span>
          <strong
            id="signature-count"
            class="is-loading"
            aria-live="polite"
          >—</strong>
          <span class="signature-unit">students have signed</span>
        </p>
      </div>

      <div style="display:flex; flex-direction:column; justify-content:center; align-items:center;">
        <a class="text-link" href={url("/volunteer/")}>
          Volunteer with us <span aria-hidden="true">-&gt;</span>
        </a>

        <div class="contact-reveal">
          <span class="contact-trigger">Contact <span aria-hidden="true">-&gt;</span></span>
          <SocialLinks />
        </div>
      </div>
    </div>
  </section>
  </div>

  <button
  class="trillium-control"
  type="button"
  aria-expanded="false"
  aria-label="Show coalition: tenants, students, labour"
>
  <span class="trillium-tooltip" aria-hidden="true">
    Tenants, students, workers
  </span>

  <img
    src={trilliumImage.src}
    alt=""
    class="trillium-mark"
  />
</button>
</Layout>

<script is:inline define:vars={{ targetDateTime, appsScriptUrl }}>
  const countdownElements = {
    days: document.getElementById("days"),
    hours: document.getElementById("hours"),
    minutes: document.getElementById("minutes"),
    seconds: document.getElementById("seconds"),
  };

  const signatureCountElement = document.getElementById("signature-count");
  const prefersReducedMotion = window.matchMedia(
    "(prefers-reduced-motion: reduce)"
  ).matches;

  function formatNumber(value) {
    return String(value).padStart(2, "0");
  }

  function formatCount(value) {
    return Number(value).toLocaleString();
  }

  function updateCountdown() {
    const targetTime = new Date(targetDateTime).getTime();
    const currentTime = Date.now();
    const remaining = Math.max(0, targetTime - currentTime);

    const totalSeconds = Math.floor(remaining / 1000);
    const days = Math.floor(totalSeconds / 86400);
    const hours = Math.floor((totalSeconds % 86400) / 3600);
    const minutes = Math.floor((totalSeconds % 3600) / 60);
    const seconds = totalSeconds % 60;

    countdownElements.days.textContent = formatNumber(days);
    countdownElements.hours.textContent = formatNumber(hours);
    countdownElements.minutes.textContent = formatNumber(minutes);
    countdownElements.seconds.textContent = formatNumber(seconds);
  }

  function easeOutCubic(t) {
    return 1 - Math.pow(1 - t, 3);
  }

  function animateSignatureCount(targetValue) {
    const finalValue = Math.max(0, Number(targetValue) || 0);

    signatureCountElement.classList.remove("is-loading");

    if (prefersReducedMotion) {
      signatureCountElement.textContent = formatCount(finalValue);
      return;
    }

    signatureCountElement.classList.add("is-animating");

    const flickerDuration = 420;
    const countDuration = 1400;
    const flickerStart = performance.now();

    function flickerFrame(now) {
      const elapsed = now - flickerStart;
      const progress = Math.min(elapsed / flickerDuration, 1);

      const fakeMax = Math.max(52, finalValue * 3);
      const jitter = Math.floor(Math.random() * fakeMax);
      signatureCountElement.textContent = formatCount(jitter);

      if (progress < 1) {
        requestAnimationFrame(flickerFrame);
      } else {
        const countStart = performance.now();

        function countFrame(frameNow) {
          const countElapsed = frameNow - countStart;
          const countProgress = Math.min(countElapsed / countDuration, 1);
          const eased = easeOutCubic(countProgress);
          const currentValue = Math.round(finalValue * eased);

          signatureCountElement.textContent = formatCount(currentValue);

          if (countProgress < 1) {
            requestAnimationFrame(countFrame);
          } else {
            signatureCountElement.textContent = formatCount(finalValue);
            signatureCountElement.classList.remove("is-animating");
          }
        }

        requestAnimationFrame(countFrame);
      }
    }

    requestAnimationFrame(flickerFrame);
  }

  async function loadSignatureCount() {
    try {
      const response = await fetch(appsScriptUrl);
      const data = await response.json();
      animateSignatureCount(data.count || 0);
    } catch (error) {
      console.error(error);
      signatureCountElement.classList.remove("is-loading");
      signatureCountElement.textContent = "0";
    }
  }

  updateCountdown();
  setInterval(updateCountdown, 1000);
  loadSignatureCount();

  const flickerNodes = [...document.querySelectorAll(".flicker-node")];
let activeIndex = 0;

function activateNode(index) {
  flickerNodes.forEach((node, nodeIndex) => {
    node.classList.toggle("is-active", nodeIndex === index);
  });

  activeIndex = index;
}

/* Start with exactly one red square. */
activateNode(0);

flickerNodes.forEach((node, index) => {
  node.addEventListener("mouseenter", () => {
    if (index !== activeIndex) return;

    const nextIndex = (activeIndex + 1) % flickerNodes.length;
    activateNode(nextIndex);
  });
});
</script>
